# Copy to .env.local for local development.
# No secrets are required for the static GitHub Pages deployment.

# Future: URL of an Anthropic API proxy serverless function.
# VITE_API_PROXY_URL=https://your-function.example.com/api/search
